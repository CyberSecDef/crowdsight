# CrowdSight backend — Python 3.12 exactly (the CAMEL/OASIS tree lags 3.13+).

# API
flask>=3.0
flask-cors>=6.0

# Inference and embeddings — SDK only, pointed at local Ollama.
openai>=1.0
httpx>=0.27

# Simulation engine
camel-ai==0.2.78
camel-oasis==0.2.5

# Knowledge graph
neo4j>=5.15

# Configuration
pydantic>=2.0
pydantic-settings>=2.2
python-dotenv>=1.0

# Validates JSON Schema dicts passed to complete_json. Pydantic covers the
# model-class form; this covers ad-hoc schemas such as Phase 3's generated
# ontology, whose shape is not known until the model proposes it.
jsonschema>=4.0

# Document ingestion
PyMuPDF>=1.24
charset-normalizer
chardet
